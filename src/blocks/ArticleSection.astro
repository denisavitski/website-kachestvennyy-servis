---
import Image, { type ImageDataProps } from '@elements/Image.astro'

export interface Props {
  heading?: string
  date?: string
  image?: ImageDataProps
  text?: string
}

const {
  heading = 'Встреча с&nbsp;управляющей организацией «Качественный Сервис» для&nbsp;обсуждения будущего Красково!',
  date = '8&nbsp;ноября 2024',
  image = { src: '/images/illustrations/5.svg' },
  text = `\
    Мы&nbsp;рады сообщить вам, что&nbsp;9&nbsp;октября в&nbsp;19:00&nbsp;в&nbsp;Красковском культурном центре, расположенном по&nbsp;адресу: ул.&nbsp;Лорха, д.&nbsp;2, состоится встреча-знакомство с&nbsp;управляющей организацией «Качественный Сервис». Это&nbsp;мероприятие открывает новые возможности для&nbsp;всех собственников квартир и&nbsp;жителей нашего посёлка. 

    <h2>Ждём вас на встрече!</h2>
  `,
} = Astro.props
---

<div class="article">
  <h1
    class="font-h1 font-mob-h1 article__heading"
    set:html={heading}
  />
  <div
    class="font-p2 font-mob-p3 article__date"
    set:html={date}
  />
  <Image
    {...image}
    lazy
    lazyBackground
    contain
    class="article__image"
  />
  <div
    class="font-p1 font-mob-p1 article__rich"
    set:html={text}
  />
</div>

<style is:global>
  .article {
    --col-width: 92.5rem;

    position: relative;
    padding: var(--section-vertical-padding) var(--screen-horisontal-padding);
    padding-top: 0;
  }

  .article__heading {
    margin-bottom: 3rem;

    @media (min-width: 769px) {
      width: var(--col-width);
    }
  }

  .article__date {
    color: var(--color-button);

    margin-bottom: 5rem;

    @media (min-width: 769px) {
      width: var(--col-width);
    }

    @media (max-width: 768px) {
      margin-bottom: 3rem;
    }
  }

  .article__image {
    @media (min-width: 769px) {
      position: absolute;
      right: var(--screen-horisontal-padding);
      top: 0;
      width: 65.5rem;
    }

    @media (max-width: 768px) {
      width: 100%;
      margin-bottom: 3rem;
    }
  }

  .article__rich {
    @media (min-width: 769px) {
      width: var(--col-width);
    }

    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
      font-size: 3.2rem;
      font-weight: 500;
      margin: 5rem 0;

      @media (max-width: 768px) {
        font-size: 2.5rem;
        font-weight: 400;
        margin: 3rem 0;
      }
    }

    ul {
      list-style: disc;
    }

    ol {
      list-style: decimal;
    }

    li {
      list-style-position: inside;
    }

    ol > li::marker {
      font-weight: 600;
    }

    ul,
    ol {
      margin: 3rem 0;
    }

    a {
      text-decoration: underline;
    }
  }
</style>
